export interface IInfoAdress {
  title: string;
  address: string;
}

export interface IInfoPost {
  id:number | string,
  createdAt:string,
  description:string,
  image:string,
  preview:string,
  title:string
}